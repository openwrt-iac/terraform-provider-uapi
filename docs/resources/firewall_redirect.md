---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "uapi_firewall_redirect Resource - uapi"
subcategory: ""
description: |-
  Firewall redirect.
---

# uapi_firewall_redirect (Resource)

Firewall redirect.

## Example Usage

```terraform
resource "uapi_firewall_redirect" "example" {
  match = {
    src_zone = "example"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `match` (Attributes) Match conditions. (see [below for nested schema](#nestedatt--match))

### Optional

- `enabled` (Boolean) Whether the entry is active.
- `id` (String) Resource id. Set it at create to choose the uci section name (e.g. `lan`); omit it to let uapi assign a prefixed ULID. Create-only: changing it forces replacement.
- `name` (String) Optional section name.
- `reflection` (Boolean) uci option reflection.
- `reflection_src` (String) uci option reflection_src.
- `reflection_zone` (List of String) uci option reflection_zone.
- `target` (String) Target / action.

### Read-Only

- `etag` (String) Opaque ETag of the resource's current state, used for If-Match optimistic concurrency.
- `managed` (Boolean) Whether the underlying uci section is uapi-managed.

<a id="nestedatt--match"></a>
### Nested Schema for `match`

Required:

- `src_zone` (String) Source firewall zone name.

Optional:

- `dest_ip` (String) Internal destination address to rewrite to.
- `dest_port` (String) Internal destination port or range to rewrite to.
- `dest_zone` (String) Destination firewall zone name.
- `family` (String) Address family: any, ipv4, or ipv6.
- `mark` (String) Match an fwmark as a value or value/mask, decimal or `0x` hex. Prefix with `!` to negate.
- `proto` (List of String) Protocols to match, by name or number (`tcp`, `udp`, `gre`, `sctp`, `47`) or a wildcard (`all`, `any`, `tcpudp`). Every protocol must be tcp or udp when a port is matched, because firewall4 keeps a port match only on those.
- `src_dip` (String) With target DNAT, the external destination address to match, which also selects the address used for NAT reflection. With target SNAT, the address to rewrite the source to, and required.
- `src_dport` (String) With target DNAT, the incoming (destination) port or range to redirect. With target SNAT, the source port to rewrite to.
- `src_ip` (String) Source IP addresses or CIDRs.
- `src_port` (String) Source ports.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import a managed firewall redirect by its stable id.
terraform import uapi_firewall_redirect.example <id>

# Importing an anonymous (unmanaged) section adopts it (renames to a stable id).
terraform import uapi_firewall_redirect.example cfg0a1b2c
```
